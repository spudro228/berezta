#pragma once

#include <cstddef>

namespace berezta {

/// Render the help screen over the full terminal.
void render_help_screen(size_t terminal_width, size_t terminal_height);

} // namespace berezta
